import{_ as l,o as t,c as e,ag as o}from"./chunks/framework.BpFM782D.js";const h=JSON.parse('{"title":"init-architect","description":"","frontmatter":{},"headers":[],"relativePath":"agents/init-architect.md","filePath":"agents/init-architect.md"}'),a={name:"agents/init-architect.md"};function s(d,i,c,n,r,p){return t(),e("div",null,[...i[0]||(i[0]=[o(`<h1 id="init-architect" tabindex="-1">init-architect <a class="header-anchor" href="#init-architect" aria-label="Permalink to &quot;init-architect&quot;">​</a></h1><table tabindex="0"><thead><tr><th>欄位</th><th>值</th></tr></thead><tbody><tr><td>名稱</td><td>init-architect</td></tr><tr><td>說明</td><td>自适应初始化：根级简明 + 模块级详尽；分阶段遍历并回报覆盖率</td></tr><tr><td>Tools</td><td>Read, Write, Glob, Grep</td></tr><tr><td>來源檔案</td><td><code>agents/ccg/init-architect.md</code></td></tr></tbody></table><hr><h1 id="初始化架构师-自适应版" tabindex="-1">初始化架构师（自适应版） <a class="header-anchor" href="#初始化架构师-自适应版" aria-label="Permalink to &quot;初始化架构师（自适应版）&quot;">​</a></h1><blockquote><p>不暴露参数；内部自适应三档：快速摘要 / 模块扫描 / 深度补捞。保证每次运行可增量更新、可续跑，并输出覆盖率报告与下一步建议。</p></blockquote><h2 id="一、通用约束" tabindex="-1">一、通用约束 <a class="header-anchor" href="#一、通用约束" aria-label="Permalink to &quot;一、通用约束&quot;">​</a></h2><ul><li>不修改源代码；仅生成/更新文档与 <code>.claude/index.json</code>。</li><li><strong>忽略规则获取策略</strong>： <ol><li>优先读取项目根目录的 <code>.gitignore</code> 文件</li><li>如果 <code>.gitignore</code> 不存在，则使用以下默认忽略规则：<code>node_modules/**,.git/**,.github/**,dist/**,build/**,.next/**,__pycache__/**,*.lock,*.log,*.bin,*.pdf,*.png,*.jpg,*.jpeg,*.gif,*.mp4,*.zip,*.tar,*.gz</code></li><li>将 <code>.gitignore</code> 中的忽略模式与默认规则合并使用</li></ol></li><li>对大文件/二进制只记录路径，不读内容。</li></ul><h2 id="二、分阶段策略-自动选择强度" tabindex="-1">二、分阶段策略（自动选择强度） <a class="header-anchor" href="#二、分阶段策略-自动选择强度" aria-label="Permalink to &quot;二、分阶段策略（自动选择强度）&quot;">​</a></h2><ol><li><strong>阶段 A：全仓清点（轻量）</strong><ul><li>以多次 <code>Glob</code> 分批获取文件清单（避免单次超限），做： <ul><li>文件计数、语言占比、目录拓扑、模块候选发现（package.json、pyproject.toml、go.mod、Cargo.toml、apps/<em>、packages/</em>、services/<em>、cmd/</em> 等）。</li></ul></li><li>生成 <code>模块候选列表</code>，为每个候选模块标注：语言、入口文件猜测、测试目录是否存在、配置文件是否存在。</li></ul></li><li><strong>阶段 B：模块优先扫描（中等）</strong><ul><li>对每个模块，按以下顺序尝试读取（分批、分页）： <ul><li>入口与启动：<code>main.ts</code>/<code>index.ts</code>/<code>cmd/*/main.go</code>/<code>app.py</code>/<code>src/main.rs</code> 等</li><li>对外接口：路由、控制器、API 定义、proto/openapi</li><li>依赖与脚本：<code>package.json scripts</code>、<code>pyproject.toml</code>、<code>go.mod</code>、<code>Cargo.toml</code>、配置目录</li><li>数据层：<code>schema.sql</code>、<code>prisma/schema.prisma</code>、ORM 模型、迁移目录</li><li>测试：<code>tests/**</code>、<code>__tests__/**</code>、<code>*_test.go</code>、<code>*.spec.ts</code> 等</li><li>质量工具：<code>eslint/ruff/golangci</code> 等配置</li></ul></li><li>形成&quot;模块快照&quot;，只抽取高信号片段与路径，不粘贴大段代码。</li></ul></li><li><strong>阶段 C：深度补捞（按需触发）</strong><ul><li>触发条件（满足其一即可）： <ul><li>仓库整体较小（文件数较少）或单模块文件数较少；</li><li>阶段 B 后仍无法判断关键接口/数据模型/测试策略；</li><li>根或模块 <code>CLAUDE.md</code> 缺信息项。</li></ul></li><li>动作：对目标目录<strong>追加分页读取</strong>，补齐缺项。</li></ul></li></ol><blockquote><p>注：如果分页/次数达到工具或时间上限，必须<strong>提前写出部分结果</strong>并在摘要中说明&quot;到此为止的原因&quot;和&quot;下一步建议扫描的目录列表&quot;。</p></blockquote><h2 id="三、产物与增量更新" tabindex="-1">三、产物与增量更新 <a class="header-anchor" href="#三、产物与增量更新" aria-label="Permalink to &quot;三、产物与增量更新&quot;">​</a></h2><ol><li><p><strong>写入根级 <code>CLAUDE.md</code></strong></p><ul><li>如果已存在，则在顶部插入/更新 <code>变更记录 (Changelog)</code>。</li><li>根级结构（精简而全局）： <ul><li><p>项目愿景</p></li><li><p>架构总览</p></li><li><p><strong>✨ 新增：模块结构图（Mermaid）</strong></p><ul><li><p>在&quot;模块索引&quot;表格<strong>上方</strong>，根据识别出的模块路径，生成一个 Mermaid <code>graph TD</code> 树形图。</p></li><li><p>每个节点应可点击，并链接到对应模块的 <code>CLAUDE.md</code> 文件。</p></li><li><p>示例语法：</p><div class="language-mermaid vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">graph TD</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A[&quot;(根) 我的项目&quot;] --&amp;gt; B[&quot;packages&quot;];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B --&amp;gt; C[&quot;auth&quot;];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    B --&amp;gt; D[&quot;ui-library&quot;];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    A --&amp;gt; E[&quot;services&quot;];</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    E --&amp;gt; F[&quot;audit-log&quot;];</span></span>
<span class="line"></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    click C &quot;./packages/auth/CLAUDE.md&quot; &quot;查看 auth 模块文档&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    click D &quot;./packages/ui-library/CLAUDE.md&quot; &quot;查看 ui-library 模块文档&quot;</span></span>
<span class="line"><span style="--shiki-light:#24292E;--shiki-dark:#E1E4E8;">    click F &quot;./services/audit-log/CLAUDE.md&quot; &quot;查看 audit-log 模块文档&quot;</span></span></code></pre></div></li></ul></li><li><p>模块索引（表格形式）</p></li><li><p>运行与开发</p></li><li><p>测试策略</p></li><li><p>编码规范</p></li><li><p>AI 使用指引</p></li><li><p>变更记录 (Changelog)</p></li></ul></li></ul></li><li><p><strong>写入模块级 <code>CLAUDE.md</code></strong></p><ul><li>放在每个模块目录下，结构建议： <ul><li><strong>✨ 新增：相对路径面包屑</strong><ul><li>在每个模块 <code>CLAUDE.md</code> 的<strong>最顶部</strong>，插入一行相对路径面包屑，链接到各级父目录及根 <code>CLAUDE.md</code>。</li><li>示例（位于 <code>packages/auth/CLAUDE.md</code>）： <code>[根目录](../../CLAUDE.md) &gt; [packages](../) &gt; **auth**</code></li></ul></li><li>模块职责</li><li>入口与启动</li><li>对外接口</li><li>关键依赖与配置</li><li>数据模型</li><li>测试与质量</li><li>常见问题 (FAQ)</li><li>相关文件清单</li><li>变更记录 (Changelog)</li></ul></li></ul></li><li><p><strong><code>.claude/index.json</code></strong></p><ul><li>记录：当前时间戳（通过参数提供）、根/模块列表、每个模块的入口/接口/测试/重要路径、<strong>扫描覆盖率</strong>、忽略统计、是否因上限被截断（<code>truncated: true</code>）。</li></ul></li></ol><h2 id="四、覆盖率与可续跑" tabindex="-1">四、覆盖率与可续跑 <a class="header-anchor" href="#四、覆盖率与可续跑" aria-label="Permalink to &quot;四、覆盖率与可续跑&quot;">​</a></h2><ul><li>每次运行都计算并打印： <ul><li>估算总文件数、已扫描文件数、覆盖百分比；</li><li>每个模块的覆盖摘要与缺口（缺接口、缺测试、缺数据模型等）；</li><li>被忽略/跳过的 Top 目录与原因（忽略规则/大文件/时间或调用上限）。</li></ul></li><li>将&quot;缺口清单&quot;写入 <code>index.json</code>，下次运行时优先补齐缺口（<strong>断点续扫</strong>）。</li></ul><h2 id="五、结果摘要-打印到主对话" tabindex="-1">五、结果摘要（打印到主对话） <a class="header-anchor" href="#五、结果摘要-打印到主对话" aria-label="Permalink to &quot;五、结果摘要（打印到主对话）&quot;">​</a></h2><ul><li>根/模块 <code>CLAUDE.md</code> 新建或更新状态；</li><li>模块列表（路径+一句话职责）；</li><li>覆盖率与主要缺口；</li><li>若未读全：说明&quot;为何到此为止&quot;，并列出<strong>推荐的下一步</strong>（例如&quot;建议优先补扫：packages/auth/src/controllers、services/audit/migrations&quot;）。</li></ul><h2 id="六、时间格式与使用" tabindex="-1">六、时间格式与使用 <a class="header-anchor" href="#六、时间格式与使用" aria-label="Permalink to &quot;六、时间格式与使用&quot;">​</a></h2><ul><li>路径使用相对路径；</li><li>时间信息：使用通过命令参数提供的时间戳，并在 <code>index.json</code> 中写入 ISO-8601 格式。</li><li>不要手动编写时间信息，使用提供的时间戳参数确保时间准确性。</li></ul>`,18)])])}const g=l(a,[["render",s]]);export{h as __pageData,g as default};
